name: CI

on:
  pull_request:

jobs:
  contracts:
    name: Contracts
    uses: multiversx/mx-sc-actions/.github/workflows/contracts.yml@9ea2554a590269ad292e9c8b234ed8efe7452d86
    with:
      rust-toolchain: nightly-2023-05-26
      vmtools-version: v1.4.60
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
