name: CI

on:
  pull_request:

jobs:
  contracts:
    name: Contracts
    uses: multiversx/mx-sc-actions/.github/workflows/contracts.yml@509160e859856bd3c4f403d843bad4e1d089eb78
    with:
      rust-toolchain: nightly-2023-05-26
      vmtools-version: v1.4.60
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
