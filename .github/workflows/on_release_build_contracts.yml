name: On release, build contracts

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build:
    uses: multiversx/mx-sc-actions/.github/workflows/reproducible-build.yml@0601fd8dfbac3284abd04df9fef42b8b8e2c1cde
    with:
      image_tag: v5.1.0
      attach_to_existing_release: true
