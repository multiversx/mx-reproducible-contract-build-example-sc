name: On release, build contracts

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build:
    uses: multiversx/mx-sc-actions/.github/workflows/reproducible-build.yml@9ea2554a590269ad292e9c8b234ed8efe7452d86
    with:
      image_tag: v5.4.0-beta.0
      attach_to_existing_release: true
      package_whole_project_src: true
